<div class="page-header">
  <h1>Stops</h1>
</div>

<div class="container">
  <ul>
    <% @stops.each do |stop| %>
      <li><a href="stops/<%= stop.id %>"><%= stop.station.name +
      " | " + stop.line.name %></a></li>
    <% end %>
  </ul>
</div>

